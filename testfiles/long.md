# A long markdown file

Five hundred lines or so, for scrolling, line numbers and find. Every
fifth section has a fenced code block, so the fence state has to survive
a long way down the file.

## Section 1

Buffer editor caret token workspace terminal offset terminal buffer byte workspace offset.

Pane workspace terminal caret caret terminal pane terminal offset caret workspace byte.

Terminal pane token token byte workspace byte byte caret workspace pane workspace.

- a list item in section 1
- another one, with `code` in it
- [ ] and a task

## Section 2

Offset editor markdown caret editor offset terminal byte markdown offset token editor.

Terminal byte byte token pane buffer terminal offset terminal byte workspace byte.

Pane gutter token offset caret buffer gutter byte gutter buffer markdown pane.

- a list item in section 2
- another one, with `code` in it
- [ ] and a task

## Section 3

Editor pane terminal byte markdown offset gutter buffer gutter markdown byte terminal.

Terminal offset caret editor buffer editor gutter caret workspace token terminal offset.

Byte buffer buffer buffer byte gutter byte gutter terminal terminal markdown gutter.

- a list item in section 3
- another one, with `code` in it
- [ ] and a task

## Section 4

Token terminal workspace markdown token byte token gutter markdown caret token buffer.

Workspace gutter buffer editor byte terminal gutter workspace pane markdown editor pane.

Caret caret gutter terminal editor gutter caret offset markdown editor caret offset.

- a list item in section 4
- another one, with `code` in it
- [ ] and a task

## Section 5

Markdown caret buffer token caret pane editor terminal editor editor pane token.

Pane workspace gutter byte editor markdown markdown workspace editor caret offset buffer.

Byte byte buffer editor offset byte token token workspace gutter token offset.

- a list item in section 5
- another one, with `code` in it
- [ ] and a task

```ts
const section = 5 // a fenced block every fifth section
```

## Section 6

Caret caret caret caret terminal gutter token caret workspace pane terminal pane.

Gutter editor terminal buffer byte workspace terminal workspace byte editor offset terminal.

Buffer byte workspace terminal pane byte caret editor token markdown buffer byte.

- a list item in section 6
- another one, with `code` in it
- [ ] and a task

## Section 7

Buffer gutter terminal terminal gutter gutter gutter gutter markdown terminal editor terminal.

Buffer markdown gutter editor offset workspace pane offset buffer editor offset workspace.

Offset markdown token terminal markdown offset buffer editor buffer pane offset offset.

- a list item in section 7
- another one, with `code` in it
- [ ] and a task

## Section 8

Offset buffer token pane byte pane pane caret pane pane offset gutter.

Buffer workspace workspace markdown gutter markdown pane byte buffer gutter buffer buffer.

Terminal pane terminal pane gutter pane buffer pane gutter byte byte workspace.

- a list item in section 8
- another one, with `code` in it
- [ ] and a task

## Section 9

Gutter token buffer token terminal token terminal caret pane gutter editor caret.

Token buffer terminal caret gutter caret terminal editor editor editor workspace editor.

Byte gutter token editor byte byte gutter token buffer editor offset offset.

- a list item in section 9
- another one, with `code` in it
- [ ] and a task

## Section 10

Editor workspace workspace token terminal offset editor caret pane pane workspace markdown.

Pane markdown offset pane byte buffer markdown offset caret editor workspace buffer.

Gutter token byte offset caret offset editor offset editor offset offset workspace.

- a list item in section 10
- another one, with `code` in it
- [ ] and a task

```ts
const section = 10 // a fenced block every fifth section
```

## Section 11

Gutter editor byte workspace editor editor editor gutter byte terminal offset workspace.

Buffer token offset offset offset gutter terminal offset workspace pane pane markdown.

Workspace terminal offset gutter offset workspace terminal gutter buffer byte offset byte.

- a list item in section 11
- another one, with `code` in it
- [ ] and a task

## Section 12

Offset pane markdown gutter offset offset gutter offset pane offset markdown offset.

Pane gutter editor caret terminal caret gutter buffer terminal token pane caret.

Terminal pane token markdown terminal editor token token buffer editor markdown editor.

- a list item in section 12
- another one, with `code` in it
- [ ] and a task

## Section 13

Gutter pane terminal caret gutter editor token pane editor caret offset caret.

Buffer caret pane buffer buffer terminal buffer workspace buffer offset gutter gutter.

Workspace caret buffer offset byte markdown offset terminal terminal pane terminal terminal.

- a list item in section 13
- another one, with `code` in it
- [ ] and a task

## Section 14

Markdown markdown workspace editor markdown editor caret token markdown caret editor offset.

Offset byte gutter buffer terminal markdown workspace editor caret terminal markdown workspace.

Token terminal markdown terminal byte pane terminal markdown terminal gutter workspace buffer.

- a list item in section 14
- another one, with `code` in it
- [ ] and a task

## Section 15

Offset caret markdown byte editor workspace offset pane terminal editor markdown workspace.

Editor pane markdown token markdown offset pane markdown gutter offset token editor.

Markdown buffer workspace markdown workspace workspace workspace offset offset pane offset gutter.

- a list item in section 15
- another one, with `code` in it
- [ ] and a task

```ts
const section = 15 // a fenced block every fifth section
```

## Section 16

Pane gutter terminal token token caret token gutter offset caret offset markdown.

Pane pane buffer pane token editor caret buffer workspace editor workspace terminal.

Token markdown caret editor workspace terminal token caret offset token markdown byte.

- a list item in section 16
- another one, with `code` in it
- [ ] and a task

## Section 17

Pane markdown workspace gutter editor editor markdown gutter workspace markdown buffer buffer.

Offset buffer pane workspace markdown pane buffer editor workspace buffer caret terminal.

Gutter markdown offset token pane pane offset workspace terminal markdown terminal editor.

- a list item in section 17
- another one, with `code` in it
- [ ] and a task

## Section 18

Caret byte workspace caret workspace markdown markdown token pane terminal byte offset.

Editor token byte caret buffer gutter editor markdown byte token editor workspace.

Offset token caret offset editor offset offset byte workspace token byte token.

- a list item in section 18
- another one, with `code` in it
- [ ] and a task

## Section 19

Token pane terminal workspace workspace editor token buffer terminal caret gutter offset.

Workspace token workspace token offset token pane gutter markdown workspace gutter terminal.

Offset offset terminal token offset terminal gutter markdown terminal markdown pane pane.

- a list item in section 19
- another one, with `code` in it
- [ ] and a task

## Section 20

Pane token gutter gutter caret terminal gutter token markdown workspace byte token.

Token pane terminal byte editor buffer markdown token markdown byte byte editor.

Workspace gutter workspace gutter markdown token terminal pane token gutter markdown offset.

- a list item in section 20
- another one, with `code` in it
- [ ] and a task

```ts
const section = 20 // a fenced block every fifth section
```

## Section 21

Markdown gutter gutter gutter terminal offset pane markdown terminal gutter workspace markdown.

Gutter terminal offset gutter markdown caret pane pane terminal byte terminal editor.

Offset markdown buffer editor byte token offset markdown terminal buffer pane gutter.

- a list item in section 21
- another one, with `code` in it
- [ ] and a task

## Section 22

Gutter caret workspace editor workspace gutter token gutter caret markdown editor caret.

Buffer caret buffer terminal buffer workspace buffer buffer caret terminal pane workspace.

Markdown markdown buffer terminal caret caret byte terminal buffer caret markdown workspace.

- a list item in section 22
- another one, with `code` in it
- [ ] and a task

## Section 23

Markdown terminal workspace token markdown token editor pane markdown caret offset buffer.

Pane buffer caret workspace token caret offset offset pane terminal workspace caret.

Gutter byte editor token markdown gutter workspace offset editor editor gutter caret.

- a list item in section 23
- another one, with `code` in it
- [ ] and a task

## Section 24

Buffer markdown markdown markdown token markdown caret token pane markdown gutter offset.

Token caret terminal editor token editor terminal pane offset gutter offset pane.

Gutter buffer gutter caret editor offset pane pane terminal editor buffer offset.

- a list item in section 24
- another one, with `code` in it
- [ ] and a task

## Section 25

Terminal buffer pane buffer markdown byte pane workspace caret caret caret offset.

Pane caret markdown buffer workspace gutter markdown byte buffer editor token offset.

Offset token pane terminal markdown pane caret caret token gutter caret markdown.

- a list item in section 25
- another one, with `code` in it
- [ ] and a task

```ts
const section = 25 // a fenced block every fifth section
```

